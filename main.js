const app = Vue.createApp({
    data() {
        return {
            product: 'Socks',
            description: 'Socks are very usefuly things that humans like to use on specific parts of their body.'
        };
    }
});